# Codegen example apps regenerate `grid.bin`, `datain/`, and `dataout/`
# via `bash regenerate.sh`. Don't commit them — some are megabytes
# (cone's grid.bin alone is ~4 MB).
*_codegen/grid.bin
*_codegen/datain/
*_codegen/dataout/
*_codegen/pdemodel.txt
